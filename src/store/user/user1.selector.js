export const selectAuthenticated = (state) => state.user1.isAuthenticated;
export const selectCurrentUser = (state) => state.user1.currentUser;
export const selectPaymentSession = (state) => state.user1.paymentSession;
